{
  "name": "amazon-stock-watcher",
  "version": "1.0.0",
  "private": true,
  "description": "Watcher locale di disponibilità Amazon.it con notifiche Telegram (nessun acquisto automatico).",
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "dev": "tsx src/index.ts",
    "build": "tsc -p tsconfig.json",
    "start": "node dist/index.js",
    "typecheck": "tsc -p tsconfig.json --noEmit && tsc -p tsconfig.test.json --noEmit",
    "test": "vitest run",
    "test:telegram": "tsx src/scripts/testTelegram.ts",
    "test:amazon": "tsx src/scripts/checkOnce.ts",
    "telegram:test": "npm run test:telegram",
    "amazon:test": "npm run test:amazon"
  },
  "dependencies": {
    "dotenv": "^18.0.3",
    "linkedom": "^0.18.13",
    "playwright": "^1.63.0"
  },
  "devDependencies": {
    "@types/node": "^24.13.6",
    "tsx": "^4.23.15",
    "typescript": "^7.0.2",
    "vitest": "^5.0.1"
  }
}
